import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "FitPulse" />
      <CardContent> Health and Fitness app catering to the growing interest in health and wellness < /CardContent>
      </Card>
    );